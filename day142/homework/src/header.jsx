import React from "react";

// Header component – shows a title for the app
 function Header() {
  // must return ONE root element (the <header>)
  return (
    <header>
      <h1>My React App</h1>
    </header>
  );
}
export default Header